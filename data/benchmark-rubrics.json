{
  "default": {
    "primaryMetrics": ["medianUs", "p95Us", "status"],
    "secondaryMetrics": ["bytes", "ops", "allocationBytes"],
    "disqualifiers": ["bad-result", "bad-apply", "timeout", "uncaught-error"],
    "rules": [
      "Do not summarize a benchmark by one aggregate winner.",
      "Keep timeout, error, unsupported, and bad-result rows visible.",
      "Record the command, runtime, fixture, and output file for every performance claim.",
      "Prefer focused benchmarks while iterating and broader baselines before publishing."
    ]
  },
  "library": {
    "primaryMetrics": ["hotPathMedianUs", "startupP95Ms", "reachableBytes"],
    "secondaryMetrics": ["exportCount", "typeBytes", "packageFiles"],
    "disqualifiers": ["wrong-result", "forbidden-import", "startup-budget-exceeded"]
  }
}
